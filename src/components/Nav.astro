<nav class="fixed top-4 left-0 right-0 z-50 pointer-events-none">
  <div class="mx-3 xl:mx-auto max-w-[1280px]">
    <div class="pointer-events-auto mx-auto max-w-[600px] w-full bg-gradient-to-b from-gray-800 to-black text-white rounded-lg flex items-center justify-between px-6 py-3 relative border border-gray-500/75 shadow-[0_6px_9px_-1px_rgba(0,0,0,0.15),0_3px_6px_-1px_rgba(0,0,0,0.09)]" style="width:calc(100vw - 24px);">
      <!-- Left: CONNECT -->
      <div class="text-sm font-medium cursor-pointer">
        CONNECT
      </div>

      <!-- Center: JUSTIN WHITE (absolutely centered) -->
      <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 text-lg font-bold pointer-events-none select-none text-shadow-sm">
        JUSTIN WHITE
      </div>

      <!-- Right: MENU -->
      <div class="text-sm font-medium cursor-pointer">
        MENU
      </div>
    </div>
  </div>
</nav>

<!-- Add padding to the top of the main content to account for the fixed nav -->
<div class="h-16"></div> 